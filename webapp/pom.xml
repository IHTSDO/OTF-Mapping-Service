<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- Module Metadata -->
  <parent>
    <groupId>org.ihtsdo.otf.mapping</groupId>
    <artifactId>mapping-parent</artifactId>
    <version>0.0.1</version>
  </parent>

  <artifactId>mapping-webapp</artifactId>
  <name>OTF RF2 Webapp</name>
  <description>OTF RF2 webapp</description>
  <packaging>war</packaging>

  <properties>
    <default.run.config>dev</default.run.config>
  </properties>

  <!-- Declare Project Dependency Versions -->
  <dependencies>

    <!-- Model interfaces. -->
    <dependency>
      <groupId>org.ihtsdo.otf.mapping</groupId>
      <artifactId>mapping-model</artifactId>
    </dependency>

  </dependencies>

  <!-- This profile configuration is designed to let this build with a default setting 
    of dev that can be overridden for prod -->
  <profiles>
    <profile>
      <id>run</id>
      <activation>
        <property>
          <name>run.config</name>
        </property>
      </activation>
      <properties>
        <default.run.config>${run.config}</default.run.config>
      </properties>
    </profile>
  </profiles>

  <!-- Standard build profile, no special configuration needed -->
  <build>
    <plugins>

      <!-- Unpack the filters.properties files -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <executions>
          <execution>
            <id>unpack</id>
            <phase>generate-sources</phase>
            <goals>
              <goal>unpack</goal>
            </goals>
            <configuration>
              <artifactItems>
                <artifactItem>
                  <groupId>org.ihtsdo.otf.mapping</groupId>
                  <artifactId>mapping-config</artifactId>
                  <version>${project.version}</version>
                  <type>zip</type>
                </artifactItem>
              </artifactItems>
              <outputDirectory>${project.build.directory}/generated-resources</outputDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-war-plugin</artifactId>
        <configuration>
          <filters>
            <filter>${project.build.directory}/generated-resources/resources/filters.properties.${default.run.config}</filter>
          </filters>
          <webResources>
            <resource>
              <directory>src/main/webapp</directory>
              <!-- enable filtering -->
              <filtering>true</filtering>
              <includes>
                <include>**/*js</include>
              </includes>
            </resource>
          </webResources>
        </configuration>
      </plugin>
     <!--  <plugin>
        <groupId>net.alchim31.maven</groupId>
        <artifactId>yuicompressor-maven-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>compress</goal>
            </goals>
          </execution>
        </executions> 
        <configuration>
          <sourceDirectory>src/main/webapp/app/js</sourceDirectory>
          <excludes>
            <exclude>**/components/angular/*js</exclude>
            <exclude>**/components/angular-bootstrap/*js</exclude>
            <exclude>**/components/angular-local-storage/*js</exclude>
            <exclude>**/components/bootstrap/*js</exclude>
            <exclude>**/components/es5-shim/*js</exclude>
            <exclude>**/components/font-awesome/*js</exclude>
            <exclude>**/components/jquery/*js</exclude>
            <exclude>**/components/jquery-splitter/*js</exclude>
            <exclude>**/components/jquery-ui/*js</exclude>
          </excludes>
        </configuration>       
      </plugin> -->
    </plugins>
  </build>

</project>
